[package]
name = "runtimetest"
version = "0.0.1"
edition = "2021"

[dependencies]
oci-spec = { version = "0.6.7", features = ["runtime"] }
nix = "0.28.0"
anyhow = "1.0"
libc = "0.2.155" # TODO (YJDoc2) upgrade to latest
nc = "0.8.21"

